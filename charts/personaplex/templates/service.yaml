apiVersion: v1
kind: Service
metadata:
  labels:
    kalavai.job.name: {{ .Values.system.jobId }}
  name: {{ .Release.Name }}-personaplex-service
spec:
  ports:
  - name: ui
    port: 8998
    protocol: TCP
  selector:
    app: {{ .Release.Name }}-personaplex
  type: NodePort
