{{- if .Values.litellm.enabled }}
apiVersion: v1
stringData:
  litellm_key: {{ .Values.litellm.key }}
kind: Secret
metadata:
  name: {{ .Release.Name }}-speaches-secret
  labels:
    kalavai.job.name: {{ .Values.system.jobId }}
type: Opaque
immutable: true
{{- end }}
