{{- if .Values.litellm.key }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.system.jobId }}-service
  labels:
    # must have this!
    kalavai.job.name: {{ .Values.system.jobId }}
spec:
  type: NodePort
  selector:
    role: leader
    kalavai.job.name: {{ .Values.system.jobId }}
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
{{- end }}